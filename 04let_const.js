/* Para buenas practicas utilizaremos notación de altasemantica
   en este caso si la variable esta formada por 2 palabras
   la primera palabra ira en minuscula y la primera letra de la segunda
   palabra tendra que estar en mayuscula */

let nombreCompleto = "Juan Perez";
console.log(nombreCompleto);

// podemos declarar más variables en una sola linea
let x, y; // definimos "x,y"
x = 10, y = 20; // Asignamos un valor para cada variable
let z = x + y; // definimos otra variable "z" con la suma de "x + y"
console.log(z); // mostramos el valor que es =